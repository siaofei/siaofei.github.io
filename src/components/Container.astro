---
/**
 * Container Component
 *
 * @param {string} [class] - Container class properties.
 */

interface Props {
  class?: string
}

const { class: className } = Astro.props
---

<div class:list={['container mx-auto px-5', className]}>
  <slot />
</div>
